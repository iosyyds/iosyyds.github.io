name: update-icon-data
on:
  schedule:
    - cron: '0 16 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'hq-icon/gen_data.py'
      - '.github/workflows/icon-data.yml'
permissions:
  contents: write
concurrency:
  group: icon-data
  cancel-in-progress: false
jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Generate icon data
        run: cd hq-icon && python3 gen_data.py
      - name: Commit and push
        run: |
          cd hq-icon
          if git diff --quiet -- data; then
            echo "数据无变化"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add data
            git commit -m "chore: 每日更新热门图标数据 [skip ci]"
            git push origin main
          fi
